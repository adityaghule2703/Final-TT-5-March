<resources>
    <!-- Your normal app theme (no windowBackground override) -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Normal app theme - no special background -->
    </style>

    <!-- Splash screen theme - used for initial launch -->
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/initial_gradient_background</item>
        <item name="android:statusBarColor">#262930</item>
        <item name="android:navigationBarColor">#262930</item>
    </style>
</resources>